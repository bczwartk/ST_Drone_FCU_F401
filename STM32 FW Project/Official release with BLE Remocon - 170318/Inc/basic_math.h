#ifndef BASIC_MATH_H
#define BASIC_MATH_H

#include "def32.h"

#define ABS(x)  (((x) > 0) ? (x) : (-(x)))

float32_t Sqrt(float32_t x);
float32_t invSqrt(float32_t x) ;


#endif /*BASIC_MATH_H */
